version: 1
frontend:
  phases:
    preBuild:
      commands:
        - echo "Building Steel Shadows - Zama Game"
    build:
      commands:
        - echo "Steel Shadows build completed"
  artifacts:
    baseDirectory: public
    files:
      - '**/*'
  cache:
    paths: []
